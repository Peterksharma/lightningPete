{
  "name": "performance-builder",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve dist",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "glob": "^10.3.3",
    "minify": "^10.1.0"
  }
}
